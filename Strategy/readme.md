  ├── core/runner.py       → run_backtest()
  ├── core/evaluator.py    → evaluate()
  ├── core/matching.py     → VWAP 撮合
  ├── core/data_loader.py  → load_data()
  ├── core/registry.py     → 策略注册 + preset 管理
  └── api/results.py       → 单个 result 的查询端点