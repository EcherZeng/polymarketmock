# Redis only — for local backend/frontend development
# Usage: docker compose -f docker-compose-redis-only.yml up -d
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data
    command: redis-server --appendonly yes

volumes:
  redis-data:
